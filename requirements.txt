# ============================================
# VERCEL DEPLOYMENT - WORKING REQUIREMENTS
# Using pre-built wheels only (no compilation)
# Python 3.9
# ============================================

# Web Framework (Pure Python - No compilation)
fastapi==0.104.1
uvicorn==0.24.0
pydantic==2.5.0
pydantic-core==2.14.1
python-multipart==0.0.6
starlette==0.27.0

# Security (Pure Python)
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
cryptography==41.0.7

# Data Processing - Use pre-built wheels
numpy==1.23.5
pandas==1.5.3
pytz==2023.3

# ML - Lightweight models
scikit-learn==1.2.2
xgboost==1.7.6

# Image Processing
Pillow==9.5.0

# Utilities
python-dotenv==1.0.0
pyyaml==6.0
requests==2.31.0
httpx==0.24.1
python-dateutil==2.8.2

# Logging
loguru==0.7.2
tqdm==4.66.1

# Remove these completely (they cause build failures):
# torch, torchvision, opencv-python, albumentations
# shap, mlflow, pyarrow, numba, llvmlite
